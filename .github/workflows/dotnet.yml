name: .NET

# This repository is a Unity project, not a standalone .NET project.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The Unity workflow is used for this repository."
